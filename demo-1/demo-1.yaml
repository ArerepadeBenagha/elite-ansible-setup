---
- host: webserver
  become: true
  become_user: root
  tasks:
  - name: install nginx
    yum:  
      name=nginx 
      state=present
  - name: start nginx service
    service:
      name=nginx
      state=started   